# ---- Core API ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# ---- Computer Vision / ML ----
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==11.0.0
torch==2.5.1
torchvision==0.20.1
# transformers pinned to 4.46.x so tokenizers stays <=0.20.3, compatible with
# chromadb 0.5.23 (which caps tokenizers). CLIP ViT-B/32 works fine on 4.46.
transformers==4.46.3
tokenizers==0.20.3
faiss-cpu==1.9.0.post1
ultralytics==8.3.55
supervision==0.25.1

# Face matching (ArcFace). insightface builds from source and needs a C/C++
# toolchain — fine in Docker (Linux) but requires MS C++ Build Tools on Windows.
# For local Windows dev without a compiler, use requirements-core.txt instead;
# the app degrades gracefully and all other search modes still work.
insightface==0.7.3
onnxruntime==1.20.1

# ---- Reports ----
reportlab==4.2.5

# ---- Live public stream ingestion (YouTube-live / HLS resolve) ----
yt-dlp==2024.12.13

# ---- Arbiter legal AI: RAG retrieval (offline) + optional Gemini generation ----
chromadb==0.5.23
google-generativeai==0.8.3

# ---- Platform: auth (JWT + bcrypt) ----
PyJWT==2.10.1
bcrypt==4.2.1
email-validator==2.2.0

# ---- Ops: server monitoring (admin system panel) ----
psutil==6.1.0

# ---- Testing ----
pytest==8.3.4
httpx==0.28.1

# ---- Utils ----
tqdm==4.67.1
